@using System.Web.Mvc
@using System.Web.Mvc.Html
@using Telerik.Web.Mvc.UI

@helper Checkbox(string id, string text)
{
    <label for="@(id)">@(text)</label>
    <input type="checkbox" name="@(id)" id="@(id)" />    
}